fn main() {
    let title = "Github";
    let price = "999"; // We are $9.99 and storing as all integer instead of float
    let duration = "monthly";
    let start_date = "14/04/2026";

    println!("Your subscription for {title} at {price} per {duration} starting {start_date}");
}
